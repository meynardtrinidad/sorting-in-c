#ifndef QUICK_SORT_H
#define QUICK_SORT_H

#include <stddef.h>

/**
 * Sorts an array of integers using quick sort algorithm
 *
 * @param arr The array to sort
 * @param size The size of the array
 */
void quick_sort(int *arr, size_t size);

#endif /* QUICK_SORT_H */
